{
  "name": "@levee/program",
  "version": "0.1.0",
  "private": true,
  "description": "Anchor workspace for the Levee on-chain program + tests.",
  "scripts": {
    "lint": "prettier --check 'tests/**/*.ts'",
    "build:mock": "anchor build -- --features mock-oracle",
    "test": "anchor test -- --features mock-oracle",
    "test:skip-build": "anchor test --skip-build"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/spl-token": "^0.4.6"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.5",
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "chai": "^4.4.1",
    "mocha": "^10.4.0",
    "prettier": "^3.2.5",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.4.0"
  }
}
