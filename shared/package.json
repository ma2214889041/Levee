{
  "name": "@levee/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, constants and region registry for the Levee monorepo.",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "regions.json"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
